# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# PHP runtime secrets — copy api/config.example.php to api/config.php locally.
api/config.php

# Local Claude Code settings (machine-specific permissions/paths).
.claude/settings.local.json
